# AAA Foundations content pack

This folder is where the first real VascEdu content pack should live.

Recommended production layout:

```text
content/aaa/
  content-pack.json
  cases.json
  questions.json
  references.json
  volumes/
    aaa-001.nrrd
  diagrams/
    aaa-neck-anatomy.svg
```

v0.1 currently keeps sample content inside `apps/desktop/src/data/sampleContent.ts` so the UI runs immediately.
